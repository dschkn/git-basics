# Clone, fetch, and pull

- `git clone` copies a repository locally.
- `git fetch` downloads new remote history without changing the working tree.
- `git pull` fetches changes and integrates them into the current branch.
